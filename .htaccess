# 随机壁纸 - 静态资源缓存与压缩（Apache 虚拟主机适用；GitHub Pages 会忽略，无副作用）
# 1) 正确 MIME
AddType image/webp .webp

# 2) 过期缓存：图片一年、清单/文案一天、HTML 一小时
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/webp  "access plus 1 year"
  ExpiresByType image/png   "access plus 1 month"
  ExpiresByType image/jpeg  "access plus 1 month"
  ExpiresByType image/gif   "access plus 1 month"
  ExpiresByType application/json "access plus 1 day"
  ExpiresByType text/plain       "access plus 1 day"
  ExpiresByType text/html        "access plus 1 hour"
</IfModule>

# 3) 不可变缓存（文件名稳定，永不重新校验）
<IfModule mod_headers.c>
  <FilesMatch "\.(webp|png|jpe?g|gif)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>
  <FilesMatch "manifest\.json$">
    Header set Cache-Control "public, max-age=86400"
  </FilesMatch>
  <FilesMatch "sw\.js$">
    Header set Cache-Control "no-cache"
  </FilesMatch>
</IfModule>

# 4) 文本资源 gzip（index.html / manifest.json / hitokoto.txt / svg），webp 已压缩不再压
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml application/json image/svg+xml
</IfModule>
