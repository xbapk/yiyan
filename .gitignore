# IDE / 编辑器配置（本地工具元数据，不入库）
.vscode/

# WorkBuddy 本地工具元数据（不入库）
.workbuddy/

# 大文件压缩包（超过 GitHub 100MB 单文件上限，不入库）
11.zip

# 壁纸压缩前的原图备份（128MB，仅本地回滚用，禁止部署）
.backup_webp/

# webp→png 替换后归档的旧 webp（44MB，仅本地回滚用，禁止部署）
.trash/
